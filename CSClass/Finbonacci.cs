using System;

namespace CSClass
{
    public class Finbonacci
    {
        /*
        private static Dictioniary<int, long> memo = new Dictioniary<int, long>();
        public static int Get(int n)
        {
            Console.WriteLine(n);

            if (n < 0) return 0;
            if (n == 1) return 1;

            if (memo.ContainKey(n))
            {
               // return memo[n];
            }
            else
            {
                long value = Get(n - 2) + Get(n - 1);
                //memo[n] = value;
                //return value;

                //return Get(n - 2) + Get(n - 1);

            }
        }
        */
    }
}